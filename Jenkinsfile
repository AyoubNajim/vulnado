pipeline {
  agent any
  tools {
    maven 'Maven'
  }
  stages {
    stage ('Initialize') {
      
    }
    
    stage ('Build') {
     steps {
     sh 'mvn clean package'
      }
    }
    
    
  }
}
